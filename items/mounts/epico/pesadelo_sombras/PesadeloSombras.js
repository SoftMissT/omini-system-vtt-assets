export class PesadeloSombrasBehavior {
  constructor(data, actor) { this.data = data; this.actor = actor; }
  async use() {
    // LÃ³gica de Toggle de Velocidade Terrestre
    ui.notifications.info("í´¥ Pesadelo invocado das sombras!");
  }
}
