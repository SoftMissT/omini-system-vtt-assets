export class GateBehavior {
  constructor(data, actor) { this.data = data; this.actor = actor; }
  async use() {
    ui.notifications.info("í¼€ Portal Dimensional Aberto!");
    // LÃ³gica para criar token de 'Portal' na cena
  }
}
